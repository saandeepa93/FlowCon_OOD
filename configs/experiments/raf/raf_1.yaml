PATHS:
  DATA_ROOT: /dataset/raf_db/basic
  VIS_PATH: ./assets/loader/

FLOW:
  N_FLOW: 1
  N_BLOCK: 8
  IN_FEAT: 512
  MLP_DIM: 256
  INIT_ZEROS: False
  DROPOUT: TRUE

DATASET:
  IN_DIST: raf
  N_CLASS: 7
  IMG_SIZE: 32
  NUM_WORKERS: 2
  AUG: True
  W_SAMPLER: True

TRAINING:
  ITER: 501
  BATCH: 128
  LR: 1e-3
  WT_DECAY: 1e-5
  MOMENTUM: 0.9
  DROPOUT: False
  PRETRAINED: resnet18
  PRT_CONFIG: 1
  PRT_LAYER: 4

LR:
  WARM: False
  ADJUST: False
  WARM_ITER: 50
  WARMUP_FROM: 1e-6
  DECAY_RATE: 0.1
  MIN_LR: 1e-6
  T_MAX: 100

LOSS:
  LMBDA_MIN: 0.07

TEST:
  EMP_PARAMS: True
  SCORE: True
  MAGNITUDE: 0.1
  IN_FEATS: [64, 128, 256, 512]

COMMENTS:
  RESNER CIFAR TRAINING with cosine scheduler
  
